# HCO3
units           real
boundary        p p p
atom_style      atomic
neighbor        1.0 bin
neigh_modify    every 10 delay 0 check no


read_data		lammps.data
#read_restart		tmp.200000 	# If the simulation encounters unphysical behavior causing metadynamics to stop, restart from the most recent restart file.

pair_style	  deepmd   Final_DT_model_nanoconfinement.pb
pair_coeff	  * *

group	move	id		<>		481		699

fix            dp_plm all plumed plumedfile plumed.dat outfile output.plumed
fix             1   move  nvt temp 		298.15	298.15 	1000  

timestep        1
thermo          100

compute  myT  move  temp 
thermo_style   custom step temp   etotal 
thermo_modify temp myT flush yes

dump mydump1 all custom  500  coord.lammpstrj id type element x  y  z 
dump_modify  mydump1 element C O H sort id 

restart        200000 tmp
run    10000000

write_data    HCO3-10ns.data

